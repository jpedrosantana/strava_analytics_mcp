# Contexto pessoal do atleta

Copie este arquivo para `CLAUDE.local.md` e preencha com seus dados.
O arquivo local é gitignored e não será commitado.

## Perfil do atleta

Parâmetros estimados pelo histórico de corridas (não de teste laboratorial).
Valores ao vivo em `athlete_config` (populados via `scripts/seed_athlete_config.py`):

- FCmáx: ??? bpm
- LTHR: ??? bpm
- FCrest: ??? bpm
- Threshold pace: ??? m/s ≈ ?:??/km
- Sexo: ???

## Contexto de Treino/Objetivo Atual

Descreva sua rotina de treinos, experiência, e objetivo atual.

## Status atual dos dados

Atualize após rodar `sync_now` e `compute-metrics`:
- Streams: ?/? atividades
- `athlete_config`: populado? (LTHR, FCmáx, FCrest, threshold_pace_mps, sex)
- Último `compute-metrics`: ????-??-??

## Ciclo atual

Descreva seu ciclo de treino atual (prova alvo, data, semana do ciclo).
